# 🍅 TomatoTimer

**TomatoTimer** - це потужний таймер продуктивності за методом Pomodoro з унікальною функцією блокування екрану під час перерв. Програма допомагає підтримувати продуктивність та здоров'я, нагадуючи про необхідність робити перерви та пропонуючи вправи для розминки.

## ✨ Основні можливості

- ⏱ **Налаштовуваний таймер Pomodoro** - встановлюйте власний час роботи та перерв
- 🔒 **Блокування екрану під час перерви** - повноекранний режим, який гарантує, що ви зробите перерву
- 💡 **20+ порад для здоров'я** - вправи для спини, шиї, очей під час перерви
- 🔊 **Налаштування звуку** - регулювання гучності та вибір мелодій
- 🚀 **Автозапуск** - програма може автоматично запускатися при старті системи
- 📊 **Статистика** - відстеження виконаних помідорів
- 🎨 **Зручний інтерфейс** - мінімалістичний дизайн на Tkinter

## 🎯 Унікальна функція: Блокування екрану

Коли закінчується робочий час, TomatoTimer **автоматично відкривається на весь екран** (аналогічно F11) і показує:
- Таймер зворотного відліку перерви
- Випадкову пораду для розминки
- Можливість перегорнути до іншої поради

Це забезпечує, що ви **дійсно відпочинете**, а не продовжите працювати!

## 📋 Вимоги

- Python 3.8 або новіший
- Tkinter (зазвичай входить у стандартну бібліотеку Python)
- pygame (опціонально, для відтворення звуку)

## 🚀 Встановлення

### 1. Клонування репозиторію або завантаження

```bash
git clone https://github.com/yourusername/TomatoTimer.git
cd TomatoTimer
```

### 2. Встановлення залежностей

```bash
pip install -r requirements.txt
```

**Примітка:** Якщо ви не плануєте використовувати власні звукові файли, pygame встановлювати не обов'язково - програма використовуватиме системні звуки.

### 3. Запуск програми

```bash
python main.py
```

## 📖 Використання

### Базове використання

1. **Запустіть програму** - подвійний клік на `main.py` або виконайте `python main.py`
2. **Натисніть "Старт"** - почнеться робочий час (за замовчуванням 25 хвилин)
3. **Працюйте** - таймер відраховує час
4. **Перерва починається автоматично** - після закінчення робочого часу екран блокується і показується порада
5. **Відпочивайте** - виконайте запропоновану вправу
6. **Перерва закінчується** - таймер автоматично повертається до стану очікування

### Налаштування

Натисніть **"⚙ Налаштування"** для доступу до:

#### Налаштування часу
- **Робочий час** - від 1 до 60 хвилин (стандарт: 25)
- **Час перерви** - від 1 до 30 хвилин (стандарт: 5)

#### Звукові налаштування
- **Гучність** - від 0 до 100%
- **Увімкнути/вимкнути звук**
- **Тестування звуку** - кнопка для перевірки

#### Інші налаштування
- **Запускати при старті системи** - автозапуск програми
- **Повноекранний режим під час перерви** - блокування екрану
- **Показувати поради** - відображення вправ

### Гарячі клавіші

- **Escape** (під час перерви) - запит на дострокове завершення перерви (потребує підтвердження)

## 📂 Структура проекту

```
TomatoTimer/
├── main.py                 # Точка входу
├── config/
│   └── default_settings.py # Налаштування за замовчуванням
├── data/
│   ├── tips.json          # База порад
│   └── sounds/            # Звукові файли
├── ui/
│   ├── main_window.py     # Головне вікно
│   ├── settings_window.py # Вікно налаштувань
│   └── break_window.py    # Повноекранне вікно перерви
├── core/
│   ├── timer.py           # Логіка таймера
│   ├── sound_manager.py   # Управління звуком
│   ├── tips_manager.py    # Управління порадами
│   └── autostart.py       # Автозапуск
├── utils/
│   └── config_manager.py  # Робота з конфігурацією
├── requirements.txt       # Залежності
└── README.md             # Документація
```

## 💡 Поради для здоров'я

Програма включає 20 порад для розминки:
- Вправи для шиї
- Розтяжка спини
- Вправи для очей (правило 20-20-20)
- Розминка плечей
- Дихальні вправи
- І багато іншого!

Ви можете додати власні поради, відредагувавши файл `data/tips.json`.

## 🔧 Налаштування порад

Відкрийте `data/tips.json` і додайте власні поради у форматі:

```json
{
  "id": 21,
  "title": "Назва вправи",
  "description": "Детальний опис того, що потрібно зробити",
  "duration": "30 секунд"
}
```

## 🎵 Додавання власних звуків

1. Помістіть звукові файли (.wav, .mp3, .ogg) у папку `data/sounds/`
2. Відкрийте налаштування і виберіть потрібний звук
3. Або змініть `work_end_sound` та `break_end_sound` у файлі конфігурації

## 🖥️ Підтримка платформ

- ✅ **Windows** - повна підтримка (включно з автозапуском)
- ✅ **macOS** - підтримка (автозапуск через Launch Agents)
- ✅ **Linux** - підтримка (автозапуск через .desktop файл)

## 📦 Створення виконуваного файлу (exe)

Для створення окремого .exe файлу використовуйте PyInstaller:

```bash
pip install pyinstaller
pyinstaller --onefile --windowed --icon=icon.ico main.py
```

Виконуваний файл буде у папці `dist/`.

## ❓ Поширені питання

### Програма не запускається?
- Переконайтеся, що встановлено Python 3.8+
- Перевірте чи встановлені всі залежності: `pip install -r requirements.txt`

### Звук не працює?
- Встановіть pygame: `pip install pygame`
- Перевірте налаштування гучності в програмі
- Увімкніть звук у налаштуваннях

### Як вимкнути блокування екрану?
- Відкрийте налаштування
- Зніміть галочку "Повноекранний режим під час перерви"

### Як змінити час роботи?
- Натисніть "⚙ Налаштування"
- Змініть "Робочий час" та "Час перерви"
- Збережіть

## 🤝 Внесок у проект

Вітаються будь-які внески! Якщо у вас є ідеї або ви знайшли баг:

1. Створіть Issue
2. Fork репозиторій
3. Створіть Pull Request

## 📝 Ліцензія

MIT License - використовуйте вільно!

## 👨‍💻 Автор

Створено з ❤️ для підвищення продуктивності та збереження здоров'я

---

**Метод Pomodoro** - це техніка управління часом, розроблена Франческо Чірілло наприкінці 1980-х років. Метод використовує таймер для поділу роботи на інтервали, традиційно 25 хвилин, розділені короткими перервами.

**TomatoTimer** допомагає вам дотримуватися цього методу та забезпечує, що ви дійсно робите перерви для збереження здоров'я та підтримки продуктивності! 🚀
